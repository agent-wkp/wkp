# Implementation milestones

Each milestone has an exit criterion that is observable, not a checklist of files. A milestone is done when its exit criterion holds on `main` with CI green. Issues carry the `M<n>` label. Order is strict: M1 replaces the Python tool for daily use before any sync or hub work starts, so every later milestone is dogfooded on a real store.

**On "holds on `main`":** `main` was deliberately frozen `v0-python` history (tagged `v0-python`, M0 task 4) through M0-M5; every milestone up to M5 actually held on `v2-rust` instead, annotated as such below in each milestone's own closing note. **(2026-09-15/16) The cutover happened**: per ADR-0013's addendum, William directed doing the `v2-rust` -> `main` cutover (issue #161) ahead of M6's remaining open tasks rather than after, so that M6's still-open pieces (#171, #175, #177) get built and released against `main` directly. `main`'s tip is now the Rust workspace, CI green, `v2-rust` deleted. M6's own exit criterion still does not fully hold (same three gaps, now tracked against `main` instead of `v2-rust`) — see M6's own section below.

**(2026-09-17) Org migration**: William created the `agent-wkp` GitHub org and had this repo transferred and renamed (`williamcaban/agent-wkp` -> `agent-wkp/wkp`; old URL redirects for clone/push), decoupling it from his personal account. The Homebrew tap moved the same way (`williamcaban/homebrew-wkp` -> `agent-wkp/homebrew-wkp`, same name -- Homebrew's own `homebrew-<name>` convention doesn't change). Rename, not just transfer, made sense here specifically: the compiled binary is already named `wkp` (`[[bin]] name = "wkp"`), so keeping the repo named `agent-wkp` inside an org *also* named `agent-wkp` would have stuttered (`agent-wkp/agent-wkp`). Every hardcoded `williamcaban/agent-wkp` reference in-repo was updated to match -- `Cargo.toml`'s `repository` field, README, the design doc's citation links, `docs/plan/kickoff.md`, `tools/pypi-tombstone/` (both its `pyproject.toml` metadata and, caught on review since a first pass missed anything outside `*.rs`/`*.toml`/`*.yml`/`*.md`/`*.rb`/`*.sh`, the actual runtime message in `tools/pypi-tombstone/src/wkp_tombstone/__init__.py`), and `deploy/systemd/wkpd.service`'s own `Documentation=` URL. `.github/workflows/release.yml` needed no changes at all, since it already derived the `ghcr.io` image path and cosign identity from `${{ github.repository_owner }}`/`${{ github.repository }}` rather than a hardcoded owner. Two things this migration does *not* retroactively fix, both permanent by design: already-published `ghcr.io/williamcaban/wkp` container images stay at that path (GHCR packages don't follow a repo transfer); and every already-published release's (`v1.0.0`-`v1.0.3`) Sigstore/Rekor transparency-log entry and embedded cosign-verify command permanently references the old repository path (Rekor entries are immutable by design). `CODEOWNERS` still reads `@williamcaban` -- that's a personal GitHub username, not an org-membership statement, so it stays valid as-is; changing it to a team is a separate governance decision, not something this migration required.

| Milestone | Theme | Exit criterion |
|---|---|---|
| M0 | Foundations | `wkp --version` builds as a static binary on macOS arm64 and Linux x86_64 from a clean checkout; CI runs fmt, clippy, test, audit, deny, bench; binary under 10 MB; minimum git version decided and enforced at runtime |
| M1 | Read path parity | On the author's real store, `wkp init`, `index`, `search`, `context`, `materialize`, `hooks --framework claude_code` work end to end; Claude Code SessionStart hook injects Tier 0; latency targets in design 4.3 met on the bench corpus; Python tool no longer used day to day |
| M2 | Write path and audit | `wkp remember` produces SSH-signed plumbing commits with provenance trailers; agent writes land in `inbox/`; `wkp promote` requires a human-signed commit; injection-corpus test passes; gitleaks rules block credential-like content |
| M3 | Local sync | Two machines and a plain bare repo (NAS or GitHub) stay in sync through `wkpd` per-device branches; the `wkp` merge driver resolves modify/delete and add/add without data loss; `git bundle` round trip works air-gapped; unsigned or unknown-signer commits are excluded from Tier 0 and 1 |
| M4 | Encryption | `visibility: private` items are age-encrypted in git via clean/smudge; keys live in macOS Keychain or Linux secret-service with ssh-agent fallback; recovery key flow; `wkp forget` rotates recipients; `wkp purge` documented and tested |
| M5 | Hub | Container serving `git http-backend` over mutually-authenticated TLS (ADR-0011 — supersedes the original sshd/`wkp-shell` transport and its "per-tenant UID" wording); per-tenant hard isolation (a container-runtime pod per tenant, ADR-0009); bare repo and SQLite index; RFC 8628 device registration issuing an mTLS device certificate; device revocation effective on next connection; integration test: register, push, revoke, push-must-fail over HTTPS+mTLS |
| M6 | Hardening and release | Landlock + seccomp on Linux, macOS sandboxing resolved as a documented limitation with an opt-in alternative (ADR-0015, issue #225) rather than a native mechanism; cosign-signed reproducible releases with SLSA provenance and SBOM; Homebrew tap; OCI image; OpenSSF Scorecard gate; self-update verifies signatures |

## M0 tasks

1. Scaffold the Cargo workspace per design 3.3 with `rust-toolchain.toml`, release profile (`lto = "fat"`, `codegen-units = 1`, `panic = "abort"`, `strip = true`), `deny.toml`, `supply-chain/` for cargo-vet, `#![forbid(unsafe_code)]` everywhere except `wkp-sys`.
2. CI baseline: fmt, clippy `-D warnings`, test, `cargo audit`, `cargo deny`, `cargo vet`, Semgrep with the Rust ruleset, actions pinned by SHA, least-privilege `permissions:`. **Done, merged 2026-09-12** (issue #2): also added `clippy::pedantic` as a separate, non-blocking advisory step (combining it with `-D warnings` in one invocation turned out to deny ~80 pre-existing pedantic violations across the workspace, not just warn on them -- a real cleanup pass of its own, out of scope here), `test --locked`, a `p/secrets` Semgrep ruleset alongside `p/rust`, an OpenSSF Scorecard workflow, and top-level `permissions: {}` (every job already declared its own grant). `docs/plan/branch-protection.md` documents the intended settings but does **not** apply them -- flipping on required CODEOWNERS review is a real policy change (see that doc's own "Why this isn't applied yet"), left for William to apply by hand.
3. Git minimum-version spike: confirm SSH signing (`gpg.format=ssh`, `gpg.ssh.allowedSignersFile`) and builtin fsmonitor behavior on the current macOS and Fedora git builds; record the decision as ADR-0001; `wkp` refuses to run below the minimum with a clear message.
4. Remove the Python tree from this branch after tagging `v0-python` on `main`; keep `AGENTS.md` (to be rewritten in M1) and `LICENSE`. **Repo-side done** (issue #4): `v0-python` tag exists on `main`; Python `src/`/`tests/`/`pyproject.toml` removed from `v2-rust` (now `main`'s own tree, post-cutover #161); `README.md` rewritten and carries the same pointer on both tag and tip; `docs/architecture.md` moved to `docs/v0/architecture.md` (PR #169); `AGENTS.md`'s own "top note" criterion is moot -- M1 task 8 already rewrote it for the v2 CLI outright, so there's no interim v0-describing version left to annotate. The literal `git grep -i python` acceptance check (expecting only 4 specific files) is stale wording from before this repo grew ~20 legitimate contextual mentions (CLAUDE.md's "no vendoring a Python dependency" rule, `git-filter-repo` being a Python tool referenced in ADR-0008/`purge.rs`, `.gitignore` Python patterns, etc.) -- verified by hand none of those hits are actual leftover Python *implementation* code, which is the check's real intent. **PyPI-side done, 2026-09-16** (William): `0.3.0` tombstone published, `0.1.0`/`0.2.0` yanked, `docs/plan/pypi-retirement.md` records what/when/why. Account-hardening (2FA, trusted publisher/token cleanup) isn't externally verifiable -- see that doc's own "Still open" note. **Issue #4 fully closed.**
5. Benchmark harness: criterion benches for cold `wkp search`, incremental index, materialize; a fixture corpus generator (5k and 50k items); `benches/baseline.json` and a regression threshold documented in `benches/README.md`. **Done, merged 2026-09-12** (issue #5, PRs #149/#150): added the missing `materialize_tier0_50k_corpus` bench and `benches/cold_start.sh` (a reproducible `wkp --version` p50/p95 measurement, not criterion-gated); `benches/baseline.json` now also records binary size and cold-start numbers for M6 to gate on later. Corpus generation stayed in `crates/wkp-core/benches/support.rs` rather than a separate `tools/gen-corpus` binary -- documented as a deliberate divergence in `benches/README.md`, not built as a redundant tool.
6. Cross-compile and static-link check: `x86_64-unknown-linux-musl`, `aarch64-apple-darwin`; size gate. **Done, merged 2026-09-11** (issue #6): also added `aarch64-unknown-linux-musl` (via `cross`, no native cross-linker for that pair on `ubuntu-latest`) beyond the two targets originally named here, an `otool -L` check on the macOS binary, a `FROM scratch` `deploy/Containerfile` proving the static-link claim end to end, and `docs/plan/build-targets.md`.

## M1 tasks

1. OKF frontmatter parser with the v2 fields (`scope`, `provenance`, `confidence`, `expires`, new `type` values); tolerant of missing or malformed frontmatter; fuzz target.
2. Index: bundled SQLite with FTS5, schema from design 5.3 (porter unicode61 content column, trigram column for identifiers, metadata columns), atomic swap of `index.db`.
3. Change detection via `git update-index --refresh` + `git status --porcelain=v2`, fsmonitor when available; hash only changed files.
4. `wkp search` with BM25, tier and budget filters, `--format paths|text|json`; cold-start budget enforced by bench.
5. `wkp context` and `wkp traverse` parity with the Python behavior (graph edges from `refs:` and wikilinks).
6. `wkp materialize --tier 0|1` with atomic write; `wkp hooks --framework claude_code` prints hook text only. **Extended 2026-09-17** (William, real usability report -- `wkp hooks --framework codex` errored with no pointer to the actual workaround): `--framework` now also accepts `codex`/`opencode`/`hermes`/`agents_md`, all four printing the same plain-text `AGENTS.md` instruction (the mechanism documented in README's own "Configure your coding agent" section, now generated instead of hand-copied). `hermes` support is backed by a real finding, not a guess: verified against Hermes Agent's own source (`agent/prompt_builder.py`, `NousResearch/hermes-agent`) that it reads `AGENTS.md` automatically too, with one caveat (a project's own `.hermes.md`/`HERMES.md` takes precedence over `AGENTS.md` for Hermes specifically) -- design 10's harness-adapter table updated to match, replacing its prior **[unverified]** note for Hermes.
7. Importer for `~/.claude/projects/*/memory/`, `CLAUDE.md`, `AGENTS.md` tagged `source: import`.
8. Golden tests for `tier0.md` output; rewrite `AGENTS.md` for the v2 CLI.
9. Optional hybrid search via `--embed-url` with RRF and BM25 fallback, kept out of the default path.

M1's exit criterion holds on `v2-rust` as of all nine tasks above merging (issues #14-22, PRs #23/#24/#25/#27/#28/#30/#31/#32/#33/#34/#35/#36). M2 tasks below are broken out per that.

## M2 tasks

1. Identity model and `allowed_signers` (design 7.3): the file format the store carries mapping a principal (`human:<id>` / `agent:<harness>[:<model>]`) to a `role` (`human`/`agent`) and an SSH public key; `wkp-git` plumbing to read and append entries; `wkp init` wires `gpg.format=ssh` and `gpg.ssh.allowedSignersFile` to point at it. Out of scope: OS keystore/Keychain ACL integration for agent private keys (that lands alongside M4's keystore work); this task assumes a key is already available to `ssh-agent` or referenced by path.
2. Signed plumbing commit wrapper in `wkp-git` (design 5.1, 7.3): a real SSH-signed commit primitive (plumbing, not the porcelain `commit_all` M1 left as a test-only placeholder) that takes a caller-specified identity (a principal from task 1), stages the given paths, and produces a commit `git verify-commit` accepts. Replaces `commit_all`'s doc-comment TODO.
3. Provenance trailers (design 5.4, 7.4): a commit-message trailer format (actor, session, source, confidence) appended by task 2's wrapper, plus a parser that reads them back off a commit for provenance queries.
4. Gitleaks-style secret detection on the write path (design 7.6): a compact, bundled ruleset (regex + entropy checks, evaluated in-process, no shelling out to a `gitleaks` binary) that refuses content matching a credential pattern and returns a redacted preview.
5. `wkp remember` (design 7.4, 7.6): reads content from stdin (never argv, per CLAUDE.md's secrets rule) plus minimal frontmatter fields, runs task 4's scan first, writes the file under `inbox/<slug>.md` with `confidence: proposed` and `provenance.source`, commits it via task 2/3's signed-plumbing-plus-trailers path.
6. Real provenance-gated tier computation (design 7.4), replacing `wkp_core::index::compute_tier`'s M1-4 type/confidence placeholder: Tier 0/1 requires the item's latest commit to be signed by a `role: human` principal (task 1), plus the `type: instruction` path-scoping rule from 7.4. Also closes the `expires` gap noticed auditing M1: an item whose `expires` date has passed is excluded from Tier 0/1 the same way an `inbox/`/agent-confidence item already is (design 5.4 says the indexer does this automatically; M1 never wired it in). Out of scope: verifying commits that arrive via sync/fetch from another machine (`wkp verify`, a fetch-time gate) — that is M3's "unsigned or unknown-signer commits are excluded from Tier 0 and 1" exit-criterion line, not this task's; this task's gate applies regardless of a commit's origin, sync just doesn't exist yet to originate one.
7. `wkp promote <path>` (design 7.4): moves an `inbox/` item into the durable tree (`user/`, `projects/<name>/`, `org/<name>/` per the 5.4 layout convention) with a human-signed commit; refuses when the calling identity isn't `role: human`, except for a harness explicitly configured `promote: auto` (7.4's documented, opt-in, not-default escape hatch).
8. Injection-corpus test suite (`tests/injection-corpus/`, CLAUDE.md: "there is a test for this; do not weaken it"): adversarial fixture stores (unsigned commits, wrong-role signers claiming `type: project-state`/`instruction`, prompt-injection payloads in otherwise-valid frontmatter, `inbox/` items claiming a tier-0 type) with a test asserting `wkp materialize --tier 0|1` never includes any of them — the regression gate for tasks 6 and 7 together.

M2's exit criterion holds on `v2-rust` as of all eight tasks above merging (issues #37-44, PRs #46-53). M3 tasks below are broken out per that.

## M3 tasks

**Scope note on "wkpd" in this milestone's own exit criterion**: design 4.2 is explicit that `wkpd` is optional convenience (file watching, scheduling) layered on top of subprocess commands that already work standalone. Tasks 1-3 below build the actual sync mechanism as a synchronous, on-demand `wkp sync` subcommand — no daemon, no file watcher — which is sufficient to demonstrate "two machines... stay in sync" for this milestone's exit criterion. Task 8 adds `wkpd` itself (watch-triggered automation calling the same `wkp sync` logic), so the milestone's own literal wording is still honored, just not load-bearing for the core guarantee.

1. Per-device identity and `sync/<device-id>` branches (design 4.2, 6.2 point 1: "avoid" via unique refs so two machines never write the same ref): a device ID generated once and persisted (e.g. `.wkp/device-id`), plus `wkp-git` plumbing to create/checkout/commit onto a per-device branch. Foundational for every later task in this milestone.
2. Custom git merge driver for OKF frontmatter (design 6.2 point 2): `wkp merge-driver` implementing git's merge-driver protocol (`%O`/`%A`/`%B` temp files), safe-mode semantics for the cases git's own three-way merge can actually hand it (add/add on the same path, frontmatter field conflicts within a modify/modify): union of `tags`, max of `updated`, keep both `provenance` entries, keep both bodies with provenance markers on an unresolved body conflict. `wkp init` writes the `.gitattributes` entry (`*.md merge=wkp`) and registers `merge.wkp.driver` in local config.
3. Modify-vs-delete safe-mode handling (design 6.2's safe-mode rule: "a deletion always loses to a modification"): git's merge machinery stops with `CONFLICT (modify/delete)` **without** invoking any content merge driver for this case, so this needs its own detection-and-resolution step (a post-merge scan via `git status`/`ls-files -u`, or an equivalent pre-merge pass): keep the modification, re-propose the deletion as a new `inbox/` item for a human to confirm — never silently drop content.
4. `wkp sync`: `git fetch` from a configured remote, merge every other device's `sync/*` branch (and `main`) into the local device branch using tasks 2/3, push the local device branch. Merging a device branch into `main` itself still needs a human-signed commit or `wkp promote` (M2-6/M2-7's provenance rules apply unchanged — an auto-committed device-branch change doesn't gain tier 0/1 just by landing in history).
5. `git bundle` round trip for air-gapped sync (design 6.1): `wkp bundle export <path> [--since <ref>]` / `wkp bundle import <path>`, wrapping `git bundle create` / `git bundle verify` plus fetch.
6. `wkp sync status` (design 6.2 point 3, "ask"): surfaces conflicts tasks 2/3 couldn't auto-resolve, as an explicit, harness-actionable report.
7. Fetch-time signature reporting: M2-6's `compute_tier` already excludes unsigned/unknown-signer commits from tier 0/1 regardless of a commit's origin (confirmed when M2-6 was built — no new gating logic needed here for the tier-gate itself to hold across sync). This task is purely human-facing visibility: after a `wkp sync`/fetch, report which newly-arrived commits are unsigned or from an unknown signer, so a human notices rather than content silently sitting at tier 2 with no explanation.
8. `wkpd`: watch-triggered automation (design 4.2's cycle: OS file watcher → debounce → incremental re-index → task 1's per-device auto-commit → task 4's `wkp sync`) over a Unix domain socket (mode `0600`, peer-UID verified, never a TCP port). A thin wrapper around tasks 1-4's already-standalone logic, not new sync logic of its own.

M3's exit criterion holds on `v2-rust` as of all eight tasks above merging (issues #54-61, PRs #63/#64/#65/#66/#67/#68/#69/#70/#72 — #70 was an unrelated, explicitly requested source-file modularization refactor that landed in the middle of this milestone's work, #71 a one-line `deny.toml` license allow-list change split out of #72 per this repo's own rule against combining that file with feature code). M4 tasks below are broken out per that.

## M4 tasks

**Scope notes, found while breaking this milestone down (verified by hand — a throwaway `age`-crate round-trip test — before writing these, not assumed from the design doc alone):**

- The `age` crate (design 7.2's chosen library) pulls in a real dependency tree regardless of feature flags — `default-features = false` does not trim it, because the size comes from `age` itself unconditionally depending on an i18n/error-message-localization stack (`fluent`/`i18n-embed` and friends), not from anything gated behind a feature. Task 1 below must measure the actual binary-size and `cargo vet` impact before merging and, if it's material, write an ADR on whether to accept it, vendor a narrower age implementation, or gate `wkp-crypto` behind a non-default feature the way M1-9 gated `--embed-url` — unlike hybrid search, encryption for `visibility: private` items is not something a user can opt out of per-item, so a feature gate here would need its own careful thought about what a build without it can and cannot do.
- Design 7.2/7.5's "keys live in macOS Keychain or Linux secret-service with ssh-agent fallback" describes SSH signing key storage (design 7.3) applied by analogy to age identities, but an age X25519 identity is a *decryption* key, not something `ssh-agent` (an SSH-agent-protocol signer) can hold or produce output from the way it holds signing keys. Task 2 below needs to resolve this design/reality gap explicitly (most likely: `ssh-agent`/an SSH key only ever signs commits per design 7.3, and the *encryption* identity is always keystore-or-file-backed with no ssh-agent-shaped fallback at all) and record the resolution as an ADR rather than silently reinterpreting the design text.
- Design 7.6's one sentence about `wkp forget` ("removes the file and, for private items, rotates the age recipients so old ciphertext becomes unreadable to revoked devices") reads as two related but distinct operations — deleting one memory item, and revoking a *device's* access to every still-existing private item — compressed into one sentence. Task 5 below should resolve which `wkp forget` actually does (or whether it is in fact two things under one name, e.g. `wkp forget <path>` vs. `wkp forget --device <id>`) and document the choice, rather than guessing silently.

1. Age encryption core in `wkp-crypto` (design 7.2): X25519 identity/recipient types and `encrypt(plaintext, recipients) -> ciphertext` / `decrypt(ciphertext, identities) -> plaintext`, using the `age` crate in-process (no shelling out). Pure functions with no git or keystore involvement yet — the primitive layer every later task builds on. Verify and record the binary-size/`cargo vet` impact noted above.
2. Device encryption identity + OS keystore integration (design 7.2, 7.3): generate a device's X25519 identity on first use; store the private key in macOS Keychain or Linux secret-service. Resolves the `ssh-agent`-fallback scope note above via its own ADR.
3. Recipients file + recovery key (design 7.2 accepted failure modes): a per-store file listing every registered device's X25519 public key plus an optional recovery public key (parallel in spirit to `allowed_signers` — tracked, not itself encrypted), generated at first use and shown once. Needed before task 4, which must know who to encrypt to.
4. git clean/smudge filter for `visibility: private` (design 7.2, 5.1): `wkp` registers as a git filter driver (`.gitattributes: *.md filter=wkp-crypt`, `wkp init` wiring `filter.wkp-crypt.clean`/`.smudge` in local config, the same self-invocation pattern `wkp merge-driver`, M3-2, already established). Clean direction parses the incoming content's frontmatter and encrypts the whole file to the current recipient list only when `visibility: private`; smudge direction detects age's own recognizable ciphertext header and decrypts only when present. Both directions are a cheap pass-through for every `shared` file.
5. `wkp forget <path>` (design 7.6): removes an item, resolving the scope note above.
6. `wkp purge` (design 7.6): a wrapped, documented `git filter-repo` procedure for true history erasure, plus the forced-push-and-re-clone-on-every-device instructions surfaced clearly to the user. "Tested" per this milestone's exit criterion means the local half (invoking `filter-repo`, verifying the target path is actually gone from history) — the multi-device re-clone step is inherently a real-world, multi-machine operation this repo's CI cannot exercise.

M4's exit criterion holds on `v2-rust` as of all six tasks above merging (issues #73-78, PRs #80/#81/#82/#84/#86/#88, plus #85 and #87 — small CI-gate fixes split out per this repo's own rule against combining `.github/workflows/*` changes with feature code). M5 tasks below are broken out per that.

## M5 tasks

**Scope notes, found while breaking this milestone down:**

- Design 8.2 mentions billing delegated to a Merchant of Record (Paddle or Lemon Squeezy). This milestone's own stated exit criterion (container with sshd + `wkp-shell` + `git http-backend`; per-tenant UID, bare repo and SQLite index; RFC 8628 device registration; device revocation effective on next connection; one integration test: register, push, revoke, push-must-fail) never mentions billing at all. Deliberately out of scope for every task below — a later, separate product decision (which MoR, pricing), not a CI-testable milestone gate.
- RFC 8628's device grant flow needs a human-facing verification page (where a user enters the short code and approves), but design 3.3 explicitly places the account/billing web app in "a separate repo... if and when it exists," walled off from tenant content. Resolution: for this milestone, the hub binary itself serves a bare-minimum verification page as part of the control plane (task 2) — good enough for the exit criterion's own CI-driven integration test — not the eventual product-quality web app, which remains out of scope here.
- Design 8.2 says the control plane's Postgres can be "Supabase or RDS" — real hosted accounts. This milestone's exit criterion only needs something CI can stand up and tear down itself (a Postgres service container in the CI job) to prove the schema, the device-grant flow and revocation actually work; provisioning a real hosted instance is later, separate deployment work this task list does not cover.
- Design 8.1 names two transports: SSH (`sshd` + `AuthorizedKeysCommand` + `wkp-shell`) and HTTPS (`git http-backend` behind a reverse proxy validating a device-scoped bearer token). The milestone's one required integration test (register, push, revoke, push-must-fail) is scoped to the SSH path only, below (task 5) — it is D7's primary transport and the simpler one to exercise in CI without also standing up TLS termination and bearer-token issuance. The HTTPS path (task 6) gets its own, independent test coverage rather than folding into that one required test.
- **Per-tenant hard isolation is a container-runtime pod per tenant (ADR-0009), not Unix-UID separation inside one shared container, and not `podman exec` from the front door into a tenant's pod** (rejected: no Kubernetes equivalent for a running service to exec into another pod on its own hot path). The front door reaches a tenant's pod over the network, speaking git's own smart-HTTP protocol against that pod's `git http-backend` — the same backend 8.1 already picked for the HTTPS transport, reused here so there is one per-tenant backend implementation, not two. Task 5 below is rescoped to the front-door container only, against a single shared filesystem; the pod-per-tenant model itself is task 7, a follow-up this milestone's exit criterion does not close without.
- **(2026-09-11, ADR-0011) The SSH transport is dropped; HTTPS with mutual TLS is the only transport.** Tasks 2, 3 and 5 below (device registration, `wkp-shell`, and the front-door SSH container/integration test) shipped and merged under the two-transport plan and are annotated in place below as superseded, not rewritten — the code they describe is scheduled for removal, not deleted from this history, and only once its mTLS replacement (tasks 8-13 below) is built and proven working. The pod-per-tenant model (ADR-0009/0010, task 7) is what actually surfaced the problem: it leaves no local repo for `wkp-shell` to exec `git-receive-pack`/`git-upload-pack` against, and bridging an SSH session to a pod's `git http-backend` over the network was judged not worth the new protocol-translation surface it would need. See `docs/adr/0011-drop-ssh-mtls-transport.md`.

1. Control-plane schema and Postgres client (design 8.2): tenants, devices (public key, `revoked_at`) and device-grant (RFC 8628 pending/approved codes) tables in `wkp-hub`, behind a real Postgres client crate — new dependency, needs its own justification, `cargo deny`/`cargo vet` pass in the PR per CLAUDE.md. CI runs this against a Postgres service container, not a hosted instance (see scope note above). The foundational piece every later task in this milestone builds on.
2. RFC 8628 device authorization grant (design 6.4, 8.1): control-plane HTTP endpoints for the device/token exchange, plus the minimal verification page resolving the scope note above; `wkp hub register` (CLI side) polls per RFC 8628, generates an ed25519 device identity via the OS keystore (same storage pattern M4-2 already established for the encryption identity, applied here to a signing key), and uploads only the public key. Test: a scripted stand-in for the human "click approve" step (a direct HTTP POST to the verification endpoint) driving a full register round trip against task 1's schema. **Superseded by task 9 (ADR-0011):** this task's "upload only the public key" enrollment shape is replaced by CSR submission and signed-certificate issuance; kept merged and historical, not reverted.
3. `wkp-shell`: key-to-tenant mapping for `sshd`'s `AuthorizedKeysCommand` (design 8.1), restricting the resulting `command=` to only `git-receive-pack`/`git-upload-pack` on that tenant's own bare repo — refuses an unknown or revoked key, and refuses any command that isn't one of those two. **Human co-sign required**: design 9.1 names "the hub's key-to-tenant mapping" explicitly as one of the changes that always needs one. Tested at the Rust logic layer (given a key and the control-plane state from task 1, what `command=` line comes out, or that nothing does); real `sshd` wiring is task 5's job, not this one's. **Superseded (ADR-0011): `crates/wkp-hub/src/wkp_shell.rs` (PR #103) is scheduled for removal** once tasks 9-11 land — the SSH transport it serves is being dropped, not just its container wiring.
4. Per-tenant bare repo, SQLite index and post-receive indexing (design 8.2, 8.3): provisioning a tenant's bare repo with `receive.fsckObjects=true`/`transfer.fsckObjects=true`, and a `post-receive` hook that runs `wkp index` against the pushed `shared` (plaintext) subset only into that tenant's own `index.db` — never touching `visibility: private` content, which stays ciphertext the hub never has a key for. Test: a real bare repo and hook fixture, a real `git push` into it, confirming the hook ran and only shared content landed in the index.
5. Front-door container image (sshd + `wkp-shell`, design 8.1/8.3) and this milestone's one required integration test, against a single shared filesystem — **not** the pod-per-tenant model (task 7). `sshd_config` hardening per 8.1's list (`PermitRootLogin no`, `PasswordAuthentication no`, `AllowAgentForwarding no`, `AllowTcpForwarding no`, `PermitTTY no`, `ed25519`-only, `AuthorizedKeysCommand` wired to task 3, `AuthorizedKeysCommandUser` set to a dedicated low-privilege user per `sshd`'s own requirement for that directive). Base image: Fedora (`quay.io/fedora/...`), built and run with `podman`. **Human co-sign required** (container/sandbox-adjacent surface, design 9.1). Integration test, run against the built container image in CI: register a device (task 2), push over SSH (tasks 3/4), revoke it (task 1's control plane), attempt another push, confirm it fails -- `sshd` re-evaluating `AuthorizedKeysCommand` on the next connection is what actually makes revocation effective, not anything client-side. **Superseded (ADR-0011): `deploy/hub/sshd_config`, the entrypoint's sshd bits, `wkp-hub-akc.sh` (PR #112), and `deploy/hub/test-ssh-integration.sh` plus its CI job (PR #114) are scheduled for removal** once tasks 9-13 land; task 13 below is this milestone's rewritten required integration test.
6. HTTPS transport (design 8.1): `git http-backend` behind a reverse proxy validating a device-scoped bearer token and setting `REMOTE_USER`. Independent test coverage (a valid token's push/fetch succeeds, an invalid or revoked one is rejected) rather than folding into task 5's one required SSH-path test, per the scope note above. **Partially superseded (ADR-0011):** `wkp-hub`'s own HTTP server and `wkp_git::http_backend` CGI wiring (merged as PR #115) carry forward unchanged; the device-scoped bearer-token check task 10 below replaces is what's superseded, not the transport itself.
7. Per-tenant pod isolation (ADR-0009 + ADR-0010, design 8.2/8.3): the control plane provisions a `podman pod` per tenant (a Kubernetes `Pod` later, same mechanism) instead of task 5/6's single shared filesystem; the front door proxies to that pod's own `git http-backend` over the network rather than running git commands locally. ADR-0010's own design pass decided: pods start on-demand per tenant with an idle-teardown reaper, overridable per-tenant to `always_warm`; each pod runs `wkp-hub` itself in a new single-tenant serving mode (not a second program); the front door reaches a tenant's pod by container-runtime DNS name on a shared user-defined network (not a per-tenant host-port mapping — no Kubernetes analog for that); bare repos persist on a bind-mounted shared path outside each pod's own ephemeral filesystem. Still open at implementation time: the idle-timeout value, cold-start request handling (queue vs. retryable error), and resource/cgroup quotas — see ADR-0010's own "not decided here" notes. Placing the indexing worker in its own no-network container in the same pod is expected to satisfy design 8.3's "indexing worker is a separate sandboxed process... with no network" line for free; confirm that during implementation rather than assuming it. **Human co-sign required** (container/sandbox surface, design 9.1). This task is what actually closes M5's "per-tenant hard isolation" exit criterion — M5 is not done until it lands. This task's own proxying logic (HTTP relay to a tenant pod's `git http-backend`) is unaffected by ADR-0011 and needs no SSH bridge, which is exactly why ADR-0011 chose to drop SSH rather than build one. **Done, merged 2026-09-11** (#142: a real cross-tenant filesystem isolation test, wired into CI — a tenant's own pod container never has any other tenant's repo bind-mounted into it at all, not just application-level path checking; PR #135, ADR-0012: the front-door container had no way to reach a container-runtime control plane to start/stop tenant pods at all, so `handle_git_http`'s on-demand pod-start — this task's own primary production code path — failed with `ENOENT` inside that container; also introduced the `PodOrchestrator` trait seam every call site now depends on instead of `Command::new("podman")` directly). Issue #110's own p50/p99 pod-proxied-vs-pre-pod-baseline latency criterion was explicitly skipped per William's direction: the pre-pod, direct-filesystem code path this would compare against no longer exists in the codebase (removed once pods/mTLS landed), so a literal before/after measurement isn't possible without reviving deleted code for a one-time number. **Correction (2026-09-13, design-doc accuracy audit):** the "confirm during implementation rather than assuming it" note above was never actually followed up on — a tenant's pod runs exactly one container (`wkp-hub serve-tenant`), handling both the network-facing `git http-backend` and the `post-receive` indexing step, not a separate no-network sandboxed indexing process as design 8.3 describes. Not a blocker to this task's own "per-tenant hard isolation" criterion (that's isolation *between* tenants, still real and tested by #142) — tracked as its own gap, **issue #159**.
8. **(ADR-0011) Hub CA and TLS termination**: the hub generates and holds its own root certificate via `rcgen`; `wkp-hub`'s HTTP server gains real `rustls`-based TLS termination (does not exist today — M5-6/task 6 shipped deliberately as plain HTTP). **Resolved (ADR-0011 addendum, 2026-09-11): the CA's private key lives in a `0600` file** matching `device_identity`'s existing fallback pattern — the database was considered and rejected, since CLAUDE.md's hard rules name only the OS keystore, a `0600` file, or stdin as approved secret sources, and this is the single highest-value secret in the system. Issue #122. **Human co-sign required** (`crates/wkp-hub/`, design 9.1).
9. **(ADR-0011) CSR-based device enrollment**: `wkp hub register`'s RFC 8628 flow (task 2) submits a CSR instead of a raw public key; the control plane signs it with the hub's CA (task 8) and returns the certificate. Device-side keypair generation and OS-keystore storage (M4-2's pattern) are unchanged — only what's uploaded and what comes back changes. Issue #123. **Human co-sign required** (`crates/wkp-hub/`, design 9.1).
10. **(ADR-0011) mTLS-authenticated git-http, per-handshake revocation check**: `handle_git_http` (`crates/wkp-hub/src/http.rs`, tasks 6/7) replaces its bearer-token check with `rustls` client-certificate verification — a custom `ClientCertVerifier` (or the stable equivalent at implementation time) checking both chain validity against the hub's own CA (task 8) and the control plane's `revoked_at` state, since standard TLS chain validation has no notion of that column; fail-closed if the control-plane check is unreachable. **Resolved (ADR-0011 addendum): `control_plane::Device`'s schema holds issued-certificate metadata (serial, issuance/expiry), `revoked_at`, and an audit trail (`revoked_by`, `reason`) — never a private key** (`bearer_token_hash` becomes obsolete). Makes revocation effective on the *next* connection only, matching the SSH path's old guarantee; force-closing an already-open connection is task 12, not this task. Issue #124. **Human co-sign required** (`crates/wkp-hub/`, design 9.1).
11. **(ADR-0011) Sequenced SSH-code removal**: once tasks 8-10 are merged and proven working end to end, delete `crates/wkp-hub/src/wkp_shell.rs` (task 3), `deploy/hub/sshd_config` and the entrypoint's sshd bits and `wkp-hub-akc.sh` (task 5), `deploy/hub/test-ssh-integration.sh` and its CI job (task 5), and the `authorized-keys-command`/`git-shell` CLI subcommands in `crates/wkp-hub/src/main.rs`. Must not land before tasks 8-10 are green — never a window with neither transport functional. Issue #125. **Human co-sign required** (`crates/wkp-hub/`, `deploy/`, `.github/`, design 9.1). **Done, merged 2026-09-11** (#138: CI job removal, sequenced first; #139: the code/deploy removal itself). CI green throughout, including task 13's own mTLS integration test running against the now-SSH-free `wkp-hub` container image.
12. **(ADR-0011 addendum) Active-connection revocation reset**: on top of task 10's per-handshake check, force-close an already-open connection for the incident-response case ("next connection" isn't good enough after a leaked credential). The front door (the only process terminating client-facing TLS, per ADR-0009/0010) keeps an in-memory registry of device ID → open connection handle, populated at the point task 10's verifier resolves identity; `wkp-hub device revoke` issues a Postgres `NOTIFY` in the same transaction that sets `revoked_at`, and the front door's `LISTEN` subscriber force-closes matching entries — no new dependency, built into Postgres. A periodic fallback sweep (piggybacking on the idle-pod reaper's own periodic loop, ADR-0010) covers a down `LISTEN` connection. Two tiers: targeted single-device close, and a separate, explicitly-audited reset-all admin action (never a side effect of a routine revoke). A force-close is a hard close — an in-flight git operation is interrupted mid-stream, not drained. Issue #128. **Human co-sign required** (`crates/wkp-hub/`, design 9.1). **Done, merged 2026-09-11** (#144). One correction found during implementation: the idle-pod reaper has never actually run as an in-process periodic loop itself (only ever invoked externally via `wkp-hub reap-idle-pods`), so there was no existing loop to piggyback on — this task's own fallback sweep is independent, driven off the same `LISTEN` subscriber's own timeout rather than a shared mechanism.
13. **(ADR-0011) This milestone's rewritten required integration test**: register a device (task 9), push and fetch over HTTPS+mTLS (task 10), revoke it (task 1's control plane), attempt another push, confirm it fails on the next TLS handshake. Replaces task 5's SSH-path integration test as the milestone's one required exit-criterion test; task 5's test stays in history as superseded, per task 11's sequencing, not deleted until task 11 lands. Issue #126. **Green in CI as of 2026-09-11** (`hub HTTPS+mTLS integration test (M5-13)` job, `.github/workflows/rust-ci.yml`), satisfying this task's own acceptance criterion that gates task 11. Getting there surfaced two real gaps beyond this task's own scope, each tracked separately rather than folded in here: **ADR-0012** (PR #135: the front-door container had no way to reach a container-runtime control plane to start/stop tenant pods at all -- a prerequisite fix, not part of this test's own design) and **issue #136** (the device identity's Ed25519 key is unusable as an mTLS client certificate through a real Ubuntu/Debian `git`, GnuTLS-linked rather than OpenSSL-linked -- CI works around it with a pinned newer-Fedora git client; #136 is the real fix, still open).
14. **(ADR-0011) Design-doc and milestone bookkeeping already done as part of landing this ADR**: `docs/adr/0011-drop-ssh-mtls-transport.md` and its 2026-09-11 addendum, the design-doc changelog and 8.1 annotation, this M5 section's rewrite. Listed as a task for traceability, not because it's still open.

**M5's own exit criterion (the table row above) holds on `v2-rust` as of 2026-09-11**, all fourteen tasks above merged (issues #89-94, #110, #116-128, ADR-0009/0010/0011/0012 and the ADR-0011 addendum; see each task's own annotation above for its specific PRs, and `agent-wkp-m5-progress`-style session history for the full blow-by-blow). Every clause of the table row's own wording is satisfied: mTLS-only transport (SSH removed, #125), per-tenant pod isolation with a real cross-tenant filesystem isolation test (#110), bare repo + SQLite index (task 4, long-since merged), RFC 8628 registration issuing an mTLS certificate (#123), revocation effective on next connection (#124) *and* against an already-open one (#128, a stronger guarantee than the table row's own literal wording asks for), and the required register/push/revoke/push-must-fail integration test green in CI (#126).

**Two real gaps surfaced along the way remain open, tracked separately, and are deliberately *not* blockers to the exit criterion above** (real production-readiness work, but outside this milestone's own stated scope): **#136** (the device identity's Ed25519 key is unusable as an mTLS client certificate through a real Ubuntu/Debian `git`, GnuTLS-linked rather than OpenSSL-linked -- a real bug for actual Ubuntu/Debian users today, not yet fixed. **Decision (William, 2026-09-14): deliberately deprioritized, not being worked on for now** -- recorded in design 6.4 and on the issue itself so it stays a known, documented limitation rather than a silently-forgotten one; revisit if it becomes a real blocker.) and **#141** (a Kubernetes `PodOrchestrator` backend -- podman-only today, `WKP_HUB_POD_ORCHESTRATOR=kubernetes` deliberately fails fast rather than half-working; required before production per William, not started). Both are M6-and-beyond hardening/production-readiness work, not this milestone's own CI-testable slice.

## Out of scope for M5 (deferred, not forgotten)

Billing/Merchant-of-Record integration (design 8.2), the account/billing web application (design 3.3's separate, not-yet-existing repo), real hosted Postgres provisioning (a Supabase or RDS account, actual credentials), and production deployment (DNS, real TLS certificates, the actual container registry/host). All later, separate work once this milestone's CI-testable exit criterion holds -- not something any task above needs to resolve.

## M6 tasks

M5's exit criterion holds (line 109 above), so M6 is breakable into tasks. All nine of M6's own exit-criterion components are now scoped below (tasks 1-9); task 10 is the `v2-rust` -> `main` cutover itself (ADR-0013, issue #161), gated on tasks 1-9 holding first, not a prerequisite to them.

**Task 8 (self-update signature verification, #177) remains deferred (William, 2026-09-14) — see its own entry below.** **Task 2 (macOS sandboxing, #171) is resolved (ADR-0015, 2026-09-16)**: not by building the native mechanism, but by a decision that no native mechanism will be built, documented as a known limitation, with an opt-in `--sandbox podman` flag tracked separately (issue #225) — see task 2's own entry below. M6's exit-criterion wording (line 15 above, updated 2026-09-16) reflects that resolution. Task 8's gap on "self-update verifies signatures" is the one remaining reason M6's own exit criterion does not yet fully hold.

**(ADR-0013 addendum, 2026-09-15) Task 10's cutover is no longer blocked on tasks 2, 6 and 8.** William directed doing the `v2-rust` -> `main` cutover now rather than waiting for #171, #175 (Homebrew tap, not yet started at the time of this note) and #177 to land — see the addendum for the full reasoning (mainly: so #175's tap gets built and released against `main`, the branch that survives, rather than against soon-to-be-retired `v2-rust`). These three tasks continue as tracked work against `main` after the cutover; M6's own exit-criterion wording still does not fully hold at cutover time, same gap, different branch.

**(2026-09-16) Task 6/#175 landed** (own entry below has the full detail) — built and released against `main` as planned. **(2026-09-16) Task 2/#171 resolved** (ADR-0015 — see its own entry below); only task 8 (#177) remains open, and M6's exit criterion does not fully hold on that one remaining count.

**Scope notes, found while breaking this milestone down:**

- Design 9.3's own gate table marks SBOM generation "(M6)" explicitly, but leaves Trivy/Grype container-image scanning and the continuous-rescan job marked only "Not started," with no M6 tag. Read literally: those two are real, tracked gaps (still open, not forgotten — see 9.3's own table) but not part of *this* milestone's exit criterion, which never mentions image scanning or rescanning. Out of scope for every task below; revisit as their own issue once M6 closes.
- Design 9.4's fuzzing section names several aspirational targets (FTS5 query construction, merge-driver input, age-filter I/O, hub webhook payloads) and continuous fuzzing via OSS-Fuzz. None of these appear in M6's own exit-criterion wording either (which is specifically about sandboxing, signed releases, and distribution channels) — out of scope for the same reason.
- Design 9.2's branch-protection and Scorecard-as-gate items overlap: applying `docs/plan/branch-protection.md`'s required-checks list to whichever branch is live is `docs/plan/branch-protection.md`'s own already-documented, deliberately-human-timed decision (see that doc's "why this isn't applied yet"), not a new M6 task — task 10's own cutover sequencing already covers applying it to `main` at the right moment. Task 9 below is specifically about Scorecard becoming an *enforcing* gate (a minimum-score check that can fail a PR), which is a distinct, still-fully-open piece of M6's own exit criterion wording ("OpenSSF Scorecard gate") that `docs/plan/branch-protection.md` does not cover.

1. **Linux process sandboxing: Landlock + seccomp** (design 7.5, issue #170). `wkp`/`wkpd` gain a Landlock ruleset restricting filesystem access to the store, the index, and git's object directory, plus a seccomp filter narrowing the syscall set to what those processes actually use. Test: a real attempt to read/write outside the allowed paths from within the sandboxed process fails at the kernel level (not an application-level check) — same "verified by hand against the real mechanism, not just unit-tested" standard `deploy/hub/test-tenant-indexing-isolation.sh` already set for issue #159's seccomp profile. No CODEOWNERS path this touches yet (a new `crates/wkp-cli` or `wkp-sys` concern) — human co-sign applies if the implementation ends up needing `unsafe` (design's own `#![forbid(unsafe_code)]` rule, `wkp-sys` is the only crate allowed it). **Done, merged 2026-09-14** (`crates/wkp-cli/src/sandbox.rs`, PR #189, no `unsafe` needed): `restrict_writes_to()` applies a Landlock write-only ruleset (`AccessFs::from_write`) to the caller's resolved store path (covers the index and git's object directory for free, since both live under it), called by `remember`/`promote`/`forget`/`index` once they've resolved that path; `restrict_dangerous_syscalls()` applies a seccomp denylist (`ptrace`, `process_vm_{read,write}v`, `mount`/`umount2`, `pivot_root`, `reboot`, module load/unload, `bpf`, and others) narrowing what the process can do rather than what path it can touch. Read/execute and the search/context/materialize hot path are untouched by design. Best-effort: an unsupported kernel logs and continues. `#![forbid(unsafe_code)]` ruled out fork/ptrace-based tests inside the crate, so verification is split — CI-automated via two hidden subcommands driven as real subprocesses (proves the write restriction and an ordinary post-filter syscall both work end-to-end), plus one hand-verified claim documented in `sandbox.rs`'s own doc comments (that `ptrace` itself gets blocked, confirmed via a standalone forked-child program outside this crate, since observing it directly needs `unsafe` FFI). `supply-chain/` exemptions for `landlock`/`seccompiler` landed first in #188, kept separate per this file's own rule against combining `supply-chain/` changes with feature code. **Regression found and fixed, 2026-09-16 (issue #227)**: the two CI-automated hidden-subcommand tests above never actually spawn a real `git` subprocess, so nobody had exercised `wkp index`/`remember`/`promote`/`forget` under a kernel that actually *enforces* this restriction until building #225's `--sandbox podman` work surfaced it by hand. Every `git` invocation opens `/dev/null` read+write during its own startup (`sanitize_stdfds()`), unconditionally, regardless of subcommand -- with only `store_path` in the allowed-write list, that dies outside it, so the sandbox didn't harden those four commands, it broke all of them on any kernel where `restrict_self()` reports `FullyEnforced`. Fixed by adding `/dev/null` to `apply_write_sandbox`'s allowed paths (`main.rs`); a new regression test (`sandbox_integration.rs`) drives the real compiled binary's real `index` subcommand against a real git repo end to end, not just the sandbox primitives in isolation -- the exact gap that let this ship as "Done" without being caught. **Caught and corrected on review (CodeRabbit, same day)**: the first version of this fix requested the full directory-capable write bundle (`AccessFs::from_write`) for the `/dev/null` rule too, same as `store_path`'s rule -- `/dev/null` isn't a directory, so the `landlock` crate silently clips that request down to what's actually valid for a non-directory target, and that silent clip is itself what was downgrading the whole ruleset from `FullyEnforced` to `PartiallyEnforced` (found by hand comparing the reported status before/after, on this repo's own dev environment). `restrict_writes_to` (`sandbox.rs`) now gives non-directory paths only `AccessFs::WriteFile`, the narrowest correct access class -- more correct regardless of environment, since it asks the kernel for exactly what a non-directory target can actually support. **Second correction, same day**: an intermediate version of the regression test hard-asserted the ruleset reaches `FullyEnforced`, which then failed for real on CI's own runner -- that kernel reports `PartiallyEnforced` even with the corrected narrow access class, different from this repo's dev environment. Design 7.5's own posture is best-effort and kernel-dependent by design, so asserting a specific enforcement level isn't something a portable test can require; the test now only asserts the command succeeds and produces a real index (the actual regression), with the enforcement level logged informationally, not asserted.
2. **macOS process sandboxing** (design 7.5, issue #171): a `sandbox-exec` profile (deprecated but still functional for CLI tools) or hardened-runtime entitlements on the signed binary, whichever is actually enforceable on the macOS version CI's `cross-compile (aarch64-apple-darwin)` job targets — design 7.5 flags this **[unverified for the specific macOS version in use; validate at build time]**, so this task's own first step is that verification, an ADR if the answer is a genuine tradeoff (e.g. `sandbox-exec` works but is deprecated with no replacement timeline), not an assumption carried into the release pipeline. **Resolved via ADR-0015 (William, 2026-09-16); #171 closed won't-fix-as-scoped.** Scoping work found a real architectural fork, not just an implementation detail: macOS has no equivalent of Landlock's `restrict_self()`-in-place API available without `unsafe`. Hardened-runtime entitlements (the release pipeline's other named option) turn out not to actually be a general filesystem-restriction mechanism at all — they govern JIT/library-validation/notarization concerns, not "deny writes outside path X," so they don't fit this task's actual need regardless of enforceability. That leaves Seatbelt (`sandbox-exec`/`sandbox_init`): the self-apply C API (`sandbox_init`) is private, undocumented, and deprecated with no committed replacement, and no well-vetted, widely-used Rust crate wraps it safely (the options found on crates.io are tiny/low-adoption, unlike `landlock`/`seccompiler`'s official-grade provenance) — `wkp-cli`'s `#![forbid(unsafe_code)]` rules out hand-rolling that FFI directly. The remaining path, re-exec'ing `wkp` under the external `/usr/bin/sandbox-exec` binary (safe Rust only, via `resolve_wkp_exe()`), is exactly the self-invocation bug class CLAUDE.md calls out by name, citing a real prior incident (a self-reinforcing fork loop that exhausted a machine's memory and process table). William's decision (ADR-0015): don't build that — document native macOS as a known limitation (design 7.5, updated), and track an opt-in `--sandbox podman` flag as issue #225 instead, which re-execs the requested subcommand inside a container rather than under a deprecated private API. Because the container's own entrypoint invokes `wkp <original args>` once without re-passing `--sandbox`, #225 carries no recursive self-invocation risk — a structurally different mechanism, not a mitigation of the same one. #225 is untracked against M6's exit criterion (opt-in, not required for it to hold) and can land on its own timeline. **#225 built, 2026-09-16** (`crates/wkp-cli/src/podman_sandbox.rs`, no `unsafe`): `wkp --sandbox podman <command>` re-execs the given subcommand inside a container, bind-mounting the caller's cwd at the same path (`-v $PWD:$PWD:Z -w $PWD`) so store-path resolution behaves the same as native. Default image is `ghcr.io/<owner>/wkp:v<version>-sandbox` (`WKP_SANDBOX_IMAGE` overrides it, for pre-release testing against a locally built image before that tag exists on a real registry) -- **not** M6-7/#176's existing `ghcr.io/<owner>/wkp:v<version>` image: that one is `FROM scratch` and has no `git` binary, so it fails immediately for anything past `--version` (found by hand testing this feature end to end). `deploy/Containerfile.sandbox` builds the real `-sandbox` tag instead: Fedora-minimal plus `git`, same `[safe] directory = *` gitconfig fix `deploy/hub/Containerfile` already uses for the same bind-mounted-repo ownership-mismatch reason. Verified against the real mechanism, not just written and assumed correct: built the image locally, ran `wkp --sandbox podman index`/`search`/`materialize` against a real fixture store end to end (real `index.db`, a real search hit, real tier0 output) -- and along the way found and fixed issue #227 (the pre-existing Landlock `/dev/null` regression, its own separate PR), since the sandboxed `index` run hit that bug too before it was fixed. Building and publishing the real `-sandbox` image tag as part of the release pipeline is `.github/workflows/release.yml` work, deliberately not bundled into this feature's own PR (CLAUDE.md's rule against combining `.github/workflows/*` changes with feature code) -- tracked as issue #229. **#229 wired, 2026-09-17**: a new `oci-image-sandbox` job, modeled directly on M6-7/#176's existing `oci-image` job, builds `deploy/Containerfile.sandbox` (not the scratch `deploy/Containerfile`) and pushes/cosign-signs it to the *same* `ghcr.io/<owner>/wkp` repository, tagged `<version>-sandbox` -- matching `podman_sandbox.rs`'s own derivation exactly, not a second image name. `publish-draft-release` now also names this image (digest) in the release notes. Amd64-only for now, same reason as the scratch image (`aarch64-unknown-linux-musl`'s build-reproducibility gap, task 3's own note). Unverified against a real tag at merge time -- this job only runs on an actual tag push (`if: github.event_name == 'push'`), so nothing in a PR can exercise it end to end; the next real release tag is what actually proves this, same as tasks 3-7's original ten-rc-tag verification pass.
3. **Reproducible release build pipeline** (design 9.6, issue #172): pinned toolchain (already true, `rust-toolchain.toml`) plus `SOURCE_DATE_EPOCH` and `--locked`, verified by building the same commit twice on independent runners and comparing artifact hashes. Foundational — tasks 4, 5, and 8 below all need a release workflow to attach to, and this is the first piece of one that doesn't exist yet (design 9.6: "No release workflow... exists today"). **Done, verified against a real tag (2026-09-14/15, William's go-ahead).** `x86_64-unknown-linux-musl` and `aarch64-apple-darwin` both build reproducibly (verified: two independent runners, byte-identical output). `aarch64-unknown-linux-musl` was found to be genuinely non-reproducible via its `cross` (Docker-based) toolchain and is temporarily dropped from this pipeline (William's decision; `rust-ci.yml`'s own cross-compile CI job still builds/smoke-tests it) — see release.yml's own comment on the `build` job's matrix for the real root-cause candidate (container-internal path vs. `--remap-path-prefix`'s host-based value), not yet fixed. Getting here from "merged, not yet verified" took ten real tag pushes (`v0.1.0-rc1` through `rc10`) and found real bugs a code-review pass never would have: GitHub Actions expression syntax parsing hyphenated `needs`/`outputs` as subtraction; `scorecard.yml`'s push trigger silently failing on every push since M6-9 (only works on the actual default branch); the build matrix's `include`+`attempt` combination silently dropping 4 of 6 build jobs; macOS runners lacking `sha256sum`; a misdiagnosed SLSA-provenance permissions issue; `podman manifest add` treating a bare `localhost/...` reference as a real registry pull; cosign unable to see podman's own login session; and `gh release create` needing an explicit `--repo` since that job never checks out the source tree. All individually fixed and merged (#193, #195, #196, #197, #198, #199, #200, #201, #202).

**First real public releases (2026-09-16):** `v0.1.0`/`v0.2.0` were already taken by the old Python package's own tags (design 9.6's distribution channels never applied to it), so the Rust binary's first real release is `v1.0.0` -- picked deliberately (not continuing the Python sequence) to signal this is the stable release the whole rewrite has been building toward. Two real bugs found running the published binaries by hand, neither caught by the rc-tag pipeline-verification pass above (which only ever checked the pipeline mechanics, not the resulting binary's own behavior): `v1.0.0`'s `--version` printed `0.1.0` (workspace `Cargo.toml` version never bumped past its M0 placeholder) -- fixed, re-released as `v1.0.1`; `v1.0.1`'s bare `wkp`/`--help` fell through to a stale M0-era placeholder message instead of real usage text -- fixed, re-released as `v1.0.2`. `v1.0.0` and `v1.0.1` both stay published (real Rekor entries, not worth unpublishing). **`v1.0.3` (2026-09-17)** is now the release actually recommended and what the Homebrew tap (task 6) points at, superseding `v1.0.2` (which also stays published, same reasoning): carries #227's Landlock `/dev/null` fix (`wkp index`/`remember`/`promote`/`forget` were broken outright under real kernel enforcement before this) and #225's opt-in `--sandbox podman` flag. Verified against the real published artifacts, not just the pipeline mechanics: downloaded both binaries, independently re-hashed them (matched the `.sha256` sidecar files), ran the real Linux binary (`--version` prints `1.0.3`; bare invocation prints real usage text, not the M0-era placeholder; `--help` lists the new `--sandbox` flag). Homebrew tap (`github.com/williamcaban/homebrew-wkp`) updated to `v1.0.3`'s URLs and checksums.
4. **Cosign signing + SLSA provenance** (design 9.6, issue #173): release artifacts (binaries, container images) signed with Sigstore cosign using keyless OIDC identities tied to the release workflow; SLSA provenance attestations generated by the build, targeting SLSA Build L3 via the official generators. Depends on task 3's release workflow existing to sign the output of. **Done, verified against a real tag** (same `v0.1.0-rc10` run as task 3): every binary is cosign-signed keylessly (Fulcio + Rekor, real public transparency-log entries now exist) and self-verified with `cosign verify-blob` in the same job; SLSA Build L3 provenance comes from `slsa-framework/slsa-github-generator`'s official generic generator (`@v2.1.0`, referenced by version tag per its own documented requirement, not SHA-pinned). The real bug this task hit wasn't the generator version at all (an earlier fix pinning to `@v1.10.0` was a misdiagnosis, reverted) — it was the calling job's `contents` permission needing to be `write`, not `read` (#196), independent of generator version.
5. **SBOM generation** (design 9.3, 9.6, issue #174): Syft producing CycloneDX and SPDX, attached to every release. The one row in 9.3's gate table explicitly tagged "(M6)" — bundles naturally with task 3/4's release workflow rather than a wholly separate pipeline. **Done, verified against a real tag** (same `v0.1.0-rc10` run): `anchore/sbom-action` scans the source tree's `Cargo.lock` (not the compiled binaries, which don't reliably embed the same dependency information), producing both formats, attached to the real draft release alongside tasks 3/4's artifacts. Design 9.6's own exit-criterion wording for this release pipeline ("reproducible... with SLSA provenance and SBOM") is now fully covered and genuinely proven across tasks 3-5, not just merged on review.
6. **Homebrew tap** (design 9.6, issue #175): a formula (own tap repo or `homebrew-core`-adjacent) for macOS and Linux, verifying checksums against task 3's release artifacts. **Done, 2026-09-16.** Own tap repo, `github.com/williamcaban/homebrew-wkp`, formula at `Formula/wkp.rb`. Targets `v1.0.2` -- `v1.0.0`'s binary reported its own `--version` as `0.1.0` (workspace `Cargo.toml` never bumped, found while building this task) and `v1.0.1`'s binary shipped an unrelated real bug (bare `wkp`/`--help` fell through to a stale M0-era placeholder message, found by William running the published binary by hand); both fixed and re-released rather than left baked into the tap. SHA-256 checksums independently re-verified for every release used (downloaded and hashed the binaries by hand, not just trusted the `.sha256` sidecar files). Two platforms only (macOS arm64, Linux x86_64) match task 3's actual build matrix; macOS Intel and Linux arm64 `on_intel`/`on_arm` branches `odie` with a clear message pointing at the real reason (Linux arm64's `cross`-toolchain reproducibility gap; macOS Intel not attempted at all) rather than silently offering a build that doesn't exist. Verified against the real mechanism throughout, not just written and assumed correct: installed Homebrew itself (unofficial user-prefix install, no root in this environment) and ran `brew tap`/`brew install`/`brew test`/`brew audit` end to end against `v1.0.1` first (`brew audit` caught one real issue, a redundant explicit `version` field duplicating what Homebrew already infers from the URL, fixed before pushing) -- then, once `v1.0.2` existed, installed `v1.0.1` for real and ran a genuine `brew upgrade williamcaban/wkp/wkp`, confirming it detected and performed the `1.0.1 -> 1.0.2` transition correctly. All three acceptance-criteria clauses (resolve, checksums, upgrade) verified against real releases, not just constructed/assumed.
7. **Multi-arch OCI image** (design 9.6, issue #176): `linux/amd64`/`linux/arm64` images from `scratch`, published as part of the release pipeline — extends M0-6's existing `deploy/Containerfile`/`docs/plan/build-targets.md` cross-compile work into an actual tagged, published release artifact rather than a CI-only build-and-discard step. **Done for `linux/amd64`, verified against a real tag; `linux/arm64` temporarily dropped** (same real-tag pass as tasks 3-5, and the same `aarch64-unknown-linux-musl` non-reproducibility gap as task 3 — this job needs that target's binary as an input). Builds and pushes a real image to `ghcr.io/<owner>/wkp`, cosign-signed by digest — a real, public `ghcr.io` push and a real Rekor entry now exist. Two real bugs found and fixed getting here: `podman manifest add localhost/wkp-amd64` was resolving as an actual registry pull attempt rather than reading local image storage (needs the explicit `containers-storage:` transport prefix, #200), and `cosign sign` couldn't see `podman login`'s credentials since the two tools check different default credential-file locations (#201). Re-add `linux/arm64` once `aarch64-unknown-linux-musl`'s reproducibility gap (task 3) is fixed.
8. **Self-update signature verification** (design 9.6, issue #177): `wkp`'s own update check verifies a candidate release's cosign signature (task 4) before applying it — the binary never trusts an unsigned or wrongly-signed update regardless of channel. Depends on tasks 3/4 existing to verify against. **Deferred (William, 2026-09-14), revisit at a later date.** Scoping this before starting found it's a bigger fork than the issue text suggests: no `wkp update`/self-update subcommand exists anywhere in the codebase yet, so this task means designing and building that command from scratch (network fetch of the latest GitHub Release, a new pure-Rust Sigstore-verification dependency, atomic binary replacement), not just adding a verification step to something that already exists — and it's security-critical (a binary deciding whether to trust and apply an attacker-supplied update), the kind of surface CLAUDE.md's CODEOWNERS co-sign rule is for. A real signed draft release now exists (`v0.1.0-rc10`, tasks 3-5/7 verified 2026-09-15) to build and test this against once it's picked back up -- the "no real release to test against" blocker this note originally cited no longer applies; it was deferred for the scope/security-review reason above, not (only) that one. Same exit-criterion consequence as task 2: M6's own wording (line 15 above) explicitly says "self-update verifies signatures," so this deferral means M6's exit criterion does not fully hold on this count either — task 10's cutover (#161) needs this task to land, or a recorded decision to descope it, same as task 2.
9. **OpenSSF Scorecard as an enforcing gate** (design 9.2, issue #178): today's Scorecard workflow (`.github/workflows/scorecard.yml`) publishes results to the Security tab only — informational, not something that can fail a PR. This task picks and applies an actual minimum-score policy (Allstar, or a custom check against the Scorecard action's own JSON output) so `M6`'s exit-criterion wording ("OpenSSF Scorecard gate," not "OpenSSF Scorecard report") is literally true, not just partially built the way 9.2 currently describes it. **Done, merged 2026-09-14**: a `score-gate` job fails if the aggregate score drops below 4 -- a threshold set from real, measured numbers (ran the actual `scorecard` CLI against this repo by hand: 7.6 on `v2-rust`, 4.3 on `main`), not guessed. Also runs on pull requests now (experimental per the action's own docs, but this repo's PRs are never forks, so the specifically-unsupported case doesn't apply) — confirmed passing for real on its own introducing PR.
10. **(ADR-0013, amended by its 2026-09-15 addendum) Migrate `v2-rust` into `main` and retire `v2-rust`**: no longer gated on tasks 2, 6 or 8 (see the addendum). **Done, 2026-09-15/16** (issue #161, PR #205; prerequisite branch-protection retarget was its own step, not folded into the PR). `main`'s branch protection was updated from the old Python `test (3.12)` check to the Rust CI job list before the content swap; the two no-op commits already on `main` (183353e/#104 merge, 3fca756 revert) were absorbed without conflict by the merge itself; `v2-rust`'s tree merged into `main` via a normal protected-branch PR (William's own review/merge, per this repo's standing rule that a PR touching `.github/` needs his co-sign even under a general merge authorization); CI on `main`'s new tip confirmed green; `v2-rust` branch deleted. One real bug found and fixed while landing this: `scorecard.yml`'s `pull_request` trigger only fired for PRs targeting `v2-rust`, so its now-required Scorecard-gate check could never post against a PR targeting `main` — the same required-check-deadlock class this task's own branch-protection step exists to avoid, just a different trigger shape; fixed by retargeting to `main` in the same PR. Follow-up cleanup, each its own small PR: stale `v2-rust` links in `tools/pypi-tombstone/` (PR #213), and dropping the now-redundant `v2-rust` branch from `rust-ci.yml`/`osv-scanner.yml` triggers (PR #214, CODEOWNERS-gated). Issue #4's PyPI retirement (yank `0.1.x`/`0.2.0`, publish the `0.3.0` tombstone) was **not** sequenced before this after all — the repo-side tombstone prep (PR #169) predates this cutover, but the actual PyPI-account actions remain undone; doing them is unaffected by ordering relative to this task, since the tombstone content itself doesn't depend on which branch is currently `main`.

Later milestones are broken into tasks when the previous milestone's exit criterion holds.
